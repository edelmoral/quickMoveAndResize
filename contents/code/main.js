client = workspace.activeClient;
if (workspace.displayWidth > 1920) {
    geo = client.geometry;
    geo.x = 1956;
    geo.y = 20;
    geo.width = 1210;
    geo.height = 680;
    client.geometry = geo;
}

